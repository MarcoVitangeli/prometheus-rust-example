# Prometheus + Grafana + Rust + Axum API sample

This is a sample for a Rust+Axum-based API that integrates with a Prometheus and visualizes the data through Grafana server. All composed with docker-compose
